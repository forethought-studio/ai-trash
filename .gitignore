.DS_Store

# Internal / private files — not for public repo
CLAUDE.md
.internal/
demo.tape

# Playwright MCP browser session artifacts (console logs, screenshots)
.playwright-mcp/

# VM testing screenshots
vm_screen*.png

# Landing page screenshots
landing-*.png

# Queue and backlog runtime state
.queue/
.backlog/
deliverables/

# Local-only PATH-shadow scan setup (the owner's machine runs the daily scan +
# sticky banner; these are personal setup, not shipped to ai-trash users).
scripts/ai-trash-banner.sh
com.ai-trash.path-shadow-scan.plist
